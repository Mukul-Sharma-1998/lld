package com.example.lld.chess.movement;

import com.example.lld.chess.Position;

public class KnightMovement implements MovementStratergy{
    @Override
    public boolean movement(Position initialPosition, Position destinationPosition) {
        return true;
    }
}
